#include <stdio.h>
#include <string.h>

int main () {
   char str[]="Hello";
   char str2[]="Zdrasti";
   int len1 = strlen(str));
   int len2 = strlen(str2);    
   int obshto = len1+len2;

   return(0);
}